# ==============================================================================
# MS-CA runtime dependencies
# ==============================================================================
# Install PyTorch FIRST, matched to your CUDA version, from pytorch.org --
# the PyPI default here is the CPU/CUDA build for your platform, which may not
# be what your GPU box wants:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ==============================================================================

# --- Core ---
torch>=2.1,<3.0          # torch.amp.GradScaler(device=...) needs >=2.1
numpy>=1.24,<3.0
pandas>=2.0,<3.0         # tensor index CSV reading

# --- Config ---
omegaconf>=2.3,<3.0      # YAML + dotlist CLI overrides + schema validation

# --- Progress / logging ---
tqdm>=4.65
wandb>=0.16              # optional at runtime; the code degrades to console-only

# NOTE: scikit-learn is deliberately NOT a dependency. MAE, RMSE, R^2 and
# Pearson r are ~15 lines of numpy in msca/utils/metrics.py, and the numpy
# implementation is already exact. Adding sklearn for four formulas would pull
# in scipy and joblib for nothing.
